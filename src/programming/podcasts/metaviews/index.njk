---
layout: layouts/base.njk
title: Metaviews Podcast
description: Episodes pulled live from the Captivate RSS feed.
---

<section class="page-head">
  <h1>Metaviews Podcast</h1>
  <p class="lede">
    A living archive of conversations—pulled directly from Captivate, rendered
    locally.
  </p>

  <div class="actions">
    <a class="btn" href="{{ metaviewsFeed.url }}">RSS feed</a>
    {% if metaviewsFeed.home %}
    <a
      class="btn btn-secondary"
      href="{{ metaviewsFeed.home }}"
      target="_blank"
      rel="noopener"
      >Captivate</a
    >
    {% endif %}
  </div>
</section>

{% if metaviewsFeed and metaviewsFeed.items and metaviewsFeed.items.length %}
<section class="episode-grid">
  {% for ep in metaviewsFeed.items %} {% set thumb = ep.image or
  (metaviewsFeed.itunesImage or "") %}
  <article class="episode-card">
    <a
      class="episode-thumb"
      href="/programming/podcasts/metaviews/episodes/{{ ep.slug }}/"
    >
      {% if ep.image %}
      <img src="{{ ep.image }}" alt="{{ ep.title }}" loading="lazy" />
      {% else %}
      <div class="thumb-fallback" aria-hidden="true"></div>
      {% endif %}
    </a>

    <div class="episode-body">
      <h2 class="episode-title">
        <a href="/programming/podcasts/metaviews/episodes/{{ ep.slug }}/"
          >{{ ep.title }}</a
        >
      </h2>

      <div class="episode-meta">
        {% if ep.date %}<span>{{ ep.date | postDate }}</span>{% endif %} {% if
        ep.duration %}<span>• {{ ep.duration }}</span>{% endif %} {% if
        ep.episodeNumber %}<span>• Ep {{ ep.episodeNumber }}</span>{% endif %}
      </div>

      {% if ep.excerpt %}
      <p class="episode-excerpt">{{ ep.excerpt }}</p>
      {% endif %}

      <div class="episode-links">
        <a
          class="link"
          href="/programming/podcasts/metaviews/episodes/{{ ep.slug }}/"
          >Episode page</a
        >
        {% if ep.link %}
        <a class="link" href="{{ ep.link }}" target="_blank" rel="noopener"
          >Captivate</a
        >
        {% endif %}
      </div>
    </div>
  </article>
  {% endfor %}
</section>
{% else %}
<p><em>No episodes found in the feed.</em></p>
{% endif %}

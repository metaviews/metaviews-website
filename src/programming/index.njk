---
layout: layouts/base.njk
title: Programming
description: Podcasts and recorded conversations produced by Metaviews.
---

<h1>Programming</h1>

<p>
  Podcasts and recorded conversations produced by Metaviews. Programming is
  time-bound, but meant to be contextualized by adjacent intelligence.
</p>

<ul>
  <li><a href="/programming/podcasts/metaviews/">Metaviews podcast</a></li>
</ul>

<hr />

<h2>Latest Metaviews episodes</h2>

{% if collections.metaviewsEpisodes and collections.metaviewsEpisodes.length %}
<ul>
  {% for ep in collections.metaviewsEpisodes %} {% if loop.index0 < 10 %}
  <li>
    <a href="{{ ep.url }}">{{ ep.data.title or ep.fileSlug }}</a>
    {% if ep.date %}<small> — {{ ep.date | date }}</small>{% endif %} {% if
    ep.data.audio %}
    <small> — <a href="{{ ep.data.audio }}">Audio</a></small>
    {% endif %}
  </li>
  {% endif %} {% endfor %}
</ul>

<p><a href="/programming/podcasts/metaviews/">All Metaviews episodes</a></p>
{% else %}
<p>No podcast episodes found yet.</p>
{% endif %}
